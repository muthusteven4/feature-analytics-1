services:
  api:
    build: .
    container_name: feature-analytics-api
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - DATABASE_URL=sqlite:////app/data/analytics.db
    volumes:
      - ./data:/app/data